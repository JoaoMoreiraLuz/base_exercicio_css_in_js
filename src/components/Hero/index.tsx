import { Formulario, HeroTitle } from './styles'
import { Container } from '../../styles'

const Hero = () => (
  <Formulario>
    <Container>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </Container>
  </Formulario>
)

export default Hero
